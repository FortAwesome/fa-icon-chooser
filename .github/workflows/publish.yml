name: Publish

on: [push]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
      with:
        node-version: '14'
    - name: Prepare Main
      run: |
        cd packages/fa-icon-chooser
        npm install
        npm run test
        npm run format.check
    - name: Prepare React
      run: |
        cd packages/fa-icon-chooser-react
        npm install
        npm run build
    - name: Publish Main - npmjs
      env:
        NPM_TOKEN: ${{ secrets.NPM_REGISTRY_ACCESS_TOKEN }}
      run: |
        cd packages/fa-icon-chooser
        npm publish --dry-run
    - name: Publish Main - Cloudsmith
      env:
        NPM_TOKEN: ${{ secrets.CLOUDSMITH_TOKEN }}
      run: |
        cd packages/fa-icon-chooser
        npm publish --registry https://npm.fontawesome.com --dry-run
    - name: Publish React - npmjs
      env:
        NPM_TOKEN: ${{ secrets.NPM_REGISTRY_ACCESS_TOKEN }}
      run: |
        cd packages/fa-icon-chooser-react
        npm publish --dry-run
    - name: Publish Main - Cloudsmith
      env:
        NPM_TOKEN: ${{ secrets.CLOUDSMITH_TOKEN }}
      run: |
        cd packages/fa-icon-chooser-react
        npm publish --registry https://npm.fontawesome.com --dry-run



